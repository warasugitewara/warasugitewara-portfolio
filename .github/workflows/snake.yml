name: generate snake animation

on:
  schedule:
    - cron: "0 */24 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: warasugitewara
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Copy SVG to public folder for GitHub Pages
        run: |
          cp dist/github-contribution-grid-snake.svg public/github-contribution-grid-snake.svg
          cp dist/github-contribution-grid-snake-dark.svg public/github-contribution-grid-snake-dark.svg

      - name: Commit and push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add dist/github-contribution-grid-snake*.svg public/github-contribution-grid-snake*.svg
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: update snake animation" && git push)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
